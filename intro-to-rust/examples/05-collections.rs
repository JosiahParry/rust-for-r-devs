fn main() {
    let arr = [1, 2, 3, 4];
    println!("Array: {:?}", arr);

    let v = vec![10, 20, 30, 40, 50];
    println!("Vector: {:?}", v);

    // Bonus: an empty i32 vector
    let _empty: Vec<i32> = Vec::new();
}
